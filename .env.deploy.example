# =============================================================
# VPS 部署用 .env 模板
# 在 VPS 上把这个文件复制成 .env 然后填实际值
# 不要 commit 到 git，不要发到聊天群
# =============================================================

# 管理员 token —— 你登录 admin 页面用的总管钥匙
# 生成方法：在 VPS 上跑 `openssl rand -base64 32`
ADMIN_TOKEN=请改成一段长随机字符串

# chatgpt2api 内部鉴权 key —— image-gen-demo 调 chatgpt2api 时用的
# 跟 ADMIN_TOKEN 不能一样，也不能跟用户密钥重叠
# 生成方法：再跑一次 `openssl rand -base64 32`
C2A_KEY=请再生一段随机字符串
